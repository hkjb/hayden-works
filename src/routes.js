import HomePage from 'pages/home.md';

const Home = {
  path: '/',
  header: 'hello',
  content: HomePage,
};

const pageArray = [Home];
const pageObject = {Home};

export default {
  pageArray,
  pageObject,
};
